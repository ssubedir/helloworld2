apiVersion: v1
kind: Pod
metadata:
  name: nginx3
  annotations:
    test.app: "hello-world-3"
spec:
  containers:
  - name: nginx
    image: docker.io/nginx:1.14.2  # Update image with docker.io registry
    ports:
    - containerPort: 80
